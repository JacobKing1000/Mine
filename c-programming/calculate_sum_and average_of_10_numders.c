#include<stdio.h>
int main(){
    int a,b;
    float num[100],Sum 0.0,Avg;
    printf("Enter the number of elements");
    scanf("%d",&n);
    for(i=0;i<n;i++)
    {
        printf("%d,Enter number:"i+1);
        scanf("%f",& num[i]);
        sum=sum+num[i];
    }
    avg=sum/n;
    printf("Sum is %f \n",Sum);
    printf("Average is %f\n",Avg)
    return 0;
}